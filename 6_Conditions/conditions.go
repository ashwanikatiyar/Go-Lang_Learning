package main

func main() {
	/*Other than the normal of use of conditions ,
	go has a special feature of initializing variable
	in the beginning of the loop , here is an eg	*/

	if age:= 20 ; age >=18{
		println("Person is Adult")
	}else if age < 18 {
		println("Person is teenager")
	}else{
		println("Person is of age =",age)
	}

	//So we can declare variable inside if construct

	//As of now Go doesnt have ternary operator , so we have to use normal if else 
}